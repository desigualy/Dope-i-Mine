# base_avatar.riv goes here

Required file:

```text
assets/avatar_rive/base_avatar.riv
```

Until that file exists, the app correctly shows the missing-rig diagnostic.

Do not replace this with SVG, PNG, CustomPaint, or blob fallback art.
