const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const m=JSON.parse(fs.readFileSync(path.join(root,'manifest','avatar_components.manifest.json'),'utf8'));let e=[];for(const c of m.components){const p=path.join(root,c.sourceSvg);if(!fs.existsSync(p))e.push('Missing '+c.component);else{const s=fs.readFileSync(p,'utf8');if(!s.includes('<svg'))e.push('Bad SVG '+c.component);if(!/(Component_|Avatar_)/.test(c.component))e.push('Bad name '+c.component);if(/(Race|Ethnicity|Religion|Nationality)/i.test(c.component))e.push('Forbidden label '+c.component);}}console.log(JSON.stringify({componentCount:m.components.length,errors:e},null,2));process.exit(e.length?1:0);